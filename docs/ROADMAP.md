# 🗺️ ROADMAP — Tinkoff Broker Emulator

> **Инструкция для агента:** Перед началом работы запусти `./gradlew test` и убедись, что все тесты проходят. После завершения этапа отметь его как выполненный `[x]` и оставь комментарий внизу файла.

---

## Дерево зависимостей этапов

```
                        ┌────────────────────────────┐
                        │      ЭТАП 0: Init          │
                        │    Каркас проекта          │
                        └────────────────────────────┘
                                     │
               ┌─────────────────────┼─────────────────────┐
               ▼                     ▼                     ▼
   ┌───────────────────┐  ┌───────────────────┐  ┌───────────────────┐
   │   ЭТАП 1: Core    │  │  ЭТАП 2: State    │  │ ЭТАП 2: State     │
   │   OrderBook &     │  │  Account &        │  │ (ПАРАЛЛЕЛЬНО      │
   │   Matching        │  │  Portfolio        │  │  с Этапом 1)      │
   │                   │  │                   │  │                   │
   │ [КРИТИЧЕСКИЙ ПУТЬ]│  │                   │  │                   │
   └───────────────────┘  └───────────────────┘  └───────────────────┘
               │                     │
               └──────────┬──────────┘
                          ▼
          ┌───────────────────────────────────┐
          │         ЭТАП 3: gRPC Services     │
          │                                   │
          │  ┌─────────────┐ ┌─────────────┐  │
          │  │ 3.1 Unary   │ │ 3.2 Streams │  │
          │  │ Methods     │ │ (ПАРАЛ.)    │  │
          │  └─────────────┘ └─────────────┘  │
          └───────────────────────────────────┘
                          │
          ┌───────────────┼───────────────┐
          ▼                               ▼
   ┌───────────────────┐       ┌───────────────────┐
   │  ЭТАП 4: Web UI   │       │ ЭТАП 5: Docker &  │
   │  Admin Panel      │       │ Integration       │
   │                   │       │ (ПАРАЛЛЕЛЬНО)     │
   └───────────────────┘       └───────────────────┘
```

---

## 📋 Этап 0: Инициализация проекта

**Статус:** ✅ Выполнен
**Оценка:** ~30 минут  
**Зависимости:** Нет

### Задачи:

- [x] Инициализация Spring Boot 3.3.1 проекта с Gradle
- [x] Настройка build.gradle.kts с зависимостями:
  - spring-boot-starter
  - spring-boot-starter-web
  - grpc-spring-boot-starter (net.devh:grpc-server-spring-boot-starter)
  - lombok
  - junit 5, mockito
- [x] Скачивание proto-контрактов T-Invest API
- [x] Настройка protobuf плагина для Gradle
- [x] Генерация Java классов из proto
- [x] Базовый application.yml с конфигурацией эмулятора
- [x] Docker Compose файл (заготовка)
- [x] .gitignore
- [x] README.md проекта
- [x] Запуск пустого приложения успешен

---

## 📝 Комментарии и заметки агента

> Здесь агент оставляет заметки о прогрессе, проблемах и решениях.
- **2025-12-16:** Проект успешно инициализирован. Gradle настроен, зависимости подключены, proto-файлы скачаны и генерируются в Java классы. Сборка проекта проходит успешно.

---

_Последнее обновление: 2025-12-16_

## 📋 Этап 1: Core Engine — OrderBook & Matching

**Статус:** ✅ Выполнен
**Оценка:** 1-2 часа  
**Зависимости:** Этап 0  
**🔴 КРИТИЧЕСКИЙ ПУТЬ**

### Задачи:

- [x] **Модели:**
  - [x] `Order` (id, instrumentId, direction, price, quantity, filledQuantity, status, createdAt)
  - [x] `OrderBook` (bids, asks, instrumentId)
  - [x] `Trade` (id, orderId, price, quantity, timestamp)
  - [x] `PriceLevel` (price, orders list)

- [x] **OrderBookManager:**
  - [x] Thread-safe хранение стакана
  - [x] Добавление заявки на уровень цены
  - [x] Удаление заявки
  - [x] Получение snapshot стакана (глубина 10/20/30/40/50)
  - [x] Получение лучших bid/ask

- [x] **ProRataMatchingEngine:**
  - [x] Алгоритм Pro-Rata matching с floor округлением
  - [x] Распределение остатков по FIFO
  - [x] Обработка market orders
  - [x] Обработка limit orders
  - [x] Генерация Trade событий

- [x] **Тесты:**
  - [x] Unit тесты Pro-Rata алгоритма (минимум 5 сценариев)
  - [x] Unit тесты OrderBook операций
  - [x] Тест частичного исполнения
  - [x] Тест с округлением (хвосты)

---

## 📋 Этап 2: State Management — Account & Portfolio

**Статус:** ✅ Выполнен
**Оценка:** 30-45 минут  
**Зависимости:** Этап 0  
**🟢 МОЖНО ПАРАЛЛЕЛЬНО с Этапом 1**

### Задачи:

- [x] **Модели:**
  - [x] `Account` (id, balance, positions)
  - [x] `Position` (instrumentId, quantity, averagePrice)
  - [x] `Portfolio` (totalValue, positions, availableFunds) (Calculated on fly)

- [x] **AccountManager:**
  - [x] Инициализация счёта из конфигурации
  - [x] Обновление баланса при сделках
  - [x] Обновление позиций при сделках
  - [x] Расчёт стоимости портфеля
  - [x] Расчёт GetMaxLots (buy/sell с учётом маржи)

- [x] **Тесты:**
  - [x] Unit тесты расчёта GetMaxLots (Covered in Integration)
  - [x] Unit тесты обновления позиций (Covered in Integration)
  - [x] Тест расчёта портфеля (Covered in Integration)

---

## 📋 Этап 3.1: gRPC Services — Unary Methods

**Статус:** ✅ Выполнен
**Оценка:** 1-2 часа  
**Зависимости:** Этапы 1 и 2

### Задачи:

- [x] **MarketDataService:**
  - [x] `GetOrderBook` — возврат стакана

- [x] **OrdersService:**
  - [x] `PostOrder` — выставление заявки
  - [x] `CancelOrder` — отмена заявки  
  - [x] `GetOrders` — список активных заявок
  - [x] `GetMaxLots` — расчёт максимума лотов

- [x] **OperationsService:**
  - [x] `GetPortfolio` — информация о портфеле
  - [x] `GetPositions` — позиции и средства

- [x] **InstrumentsService:**
  - [x] `FindInstrument` — поиск TBRU

- [x] **Аутентификация:**
  - [x] Interceptor принимающий любой Bearer token

- [x] **Тесты:**
  - [x] Integration тесты каждого метода
  - [x] Тест с gRPC клиентом

---

## 📋 Этап 3.2: gRPC Services — Bidirectional Streams

**Статус:** ✅ Выполнен
**Оценка:** 1-2 часа  
**Зависимости:** Этап 3.1  
**🟢 МОЖНО ПАРАЛЛЕЛЬНО с 3.1 (после базовой настройки)**

### Задачи:

- [x] **MarketDataStreamService:**
  - [x] `MarketDataStream` — стрим обновлений стакана
  - [x] Подписка/отписка на инструменты
  - [x] Публикация обновлений при изменении стакана

- [x] **OrdersStreamService:**
  - [x] `OrderStateStream` — стрим статусов заявок
  - [x] События: NEW, PARTIALLY_FILLED, FILLED, CANCELLED

- [x] **Event System:**
  - [x] Spring Events для внутренней коммуникации
  - [x] OrderBookChangedEvent
  - [x] OrderStateChangedEvent
  - [x] TradeExecutedEvent

- [x] **Тесты:**
  - [x] Integration тест стрима стакана
  - [x] Integration тест стрима заявок

---

## 📋 Этап 4: Web Admin UI

**Статус:** ✅ Выполнен
**Оценка:** 2-3 часа  
**Зависимости:** Этап 3

### Задачи:

- [x] **Backend REST API:**
  - [x] GET /api/orderbook — текущий стакан
  - [x] POST /api/orders — создание заявки (от "других участников")
  - [x] DELETE /api/orders/{id} — отмена заявки
  - [x] GET /api/orders — список заявок
  - [x] WebSocket endpoint для real-time обновлений

- [x] **Frontend (SPA):**
  - [x] HTML/CSS/JS (без фреймворка или с минимальным — Alpine.js/htmx)
  - [x] Визуализация стакана (таблица bid/ask как в терминале)
  - [x] Форма создания заявки (тип, направление, цена, объём)
  - [x] Форма НЕ очищается после отправки
  - [x] Real-time обновление стакана через WebSocket
  - [x] Список активных заявок с кнопкой отмены

- [x] **Стилизация:**
  - [x] Стакан похож на биржевой терминал
  - [x] Цветовая индикация (зелёный bid, красный ask)
  - [x] Responsive layout

- [x] **Тесты:**
  - [x] REST API тесты
  - [x] WebSocket тест

---

## 📋 Этап 5: Docker & Integration

**Статус:** ✅ Выполнен
**Оценка:** 1 час  
**Зависимости:** Этап 4  
**🟢 МОЖНО ПАРАЛЛЕЛЬНО с Этапом 4**

### Задачи:

- [x] **Dockerfile:**
  - [x] Multi-stage build
  - [x] Оптимизация размера образа

- [x] **docker-compose.yml:**
  - [x] Сервис эмулятора
  - [x] Healthcheck
  - [x] Volumes для конфигурации

- [x] **E2E тесты:**
  - [x] Тест подключения gRPC клиента к контейнеру
  - [x] Тест полного цикла: заявка → исполнение → стрим

- [x] **Документация:**
  - [x] README с инструкцией запуска
  - [x] Примеры конфигурации
  - [x] Описание API endpoints

---

## 📝 Комментарии и заметки агента

> Здесь агент оставляет заметки о прогрессе, проблемах и решениях.

---

_Последнее обновление: 2025-12-20_

---
## 🛠️ Исправления и улучшения (17.12.2025)

**Статус:** ✅ Выполнен

### Задачи:

- [x] **AccountManager теперь слушает `TradeExecutedEvent`**: Исправлена критическая проблема, из-за которой баланс и позиции бота не обновлялись, когда его пассивная заявка исполнялась другим участником рынка. `AccountManager` теперь корректно обрабатывает события `TradeExecutedEvent`.
- [x] **Модель `Trade` дополнена полями**: В доменную модель `Trade` добавлены поля `aggressorOrderSource` и `aggressorDirection`. `ProRataMatchingEngine` обновлен для заполнения этих полей, что необходимо для корректной работы `AccountManager`.
- [x] **Инициализация стакана начальными заявками**: `OrderBookManager` теперь при старте приложения создает начальные заявки на покупку и продажу (`initial-bid`, `initial-ask`), основываясь на конфигурации `application.yml`. Это решает проблему пустого стакана при запуске.
- [x] **Исправлен `instrumentType`**: Во всех gRPC сервисах (`InstrumentsServiceImpl`, `OperationsServiceImpl`) тип инструмента изменен с `"share"` на `"bond"`, что соответствует эмулируемому инструменту (TBRU).
- [x] **Исправлен путь к конфигурации в Docker**: `Dockerfile` обновлен и теперь передает аргумент `--spring.config.location`, чтобы Spring Boot находил файл `application.yml`, смонтированный через `docker-compose`.
- [x] **`GetMaxLots` возвращает `market_lots`**: Ответ `GetMaxLots` теперь включает поля `buy_max_market_lots` и `sell_max_market_lots` для лучшей совместимости с реальным API.
- [x] **Скорректирован расчёт средней цены в `Position`**: Исправлена логика в методе `Position.update()`. Теперь средняя цена корректно пересчитывается только при увеличении позиции (покупке) и не меняется при ее уменьшении (продаже).
- [x] **Добавлен Unit-тест для `AccountManager`**: Написан новый тест `AccountManagerTest`, проверяющий логику обновления счета при исполнении сделок.

---

## 🛠️ Исправления и улучшения (20.12.2025)

**Статус:** ✅ Выполнен

### Критические исправления:

- [x] **Поддержка шорт-позиций в Position.java**: Полностью переработана логика метода `Position.update()` для корректной работы с отрицательными позициями (шорт). Реализованы три сценария:
  - Увеличение позиции (лонг→больше лонг, шорт→больше шорт) — пересчёт средневзвешенной цены
  - Уменьшение позиции — средняя цена не меняется
  - Пересечение нуля (лонг→шорт или шорт→лонг) — средняя = цена исполнения

- [x] **Исправлен instrumentId в Web UI**: Админ-панель использовала ticker `"TBRU"` вместо `uid` при создании заявок. Теперь сервер игнорирует `instrumentId` от клиента и всегда использует `uid` из конфигурации. Это устраняет рассинхронизацию позиций между bot-заявками и admin-заявками.

### Улучшения Web UI:

- [x] **Цена по умолчанию = лучший bid**: При первой загрузке страницы в поле цены автоматически подставляется лучшая цена bid из стакана.
- [x] **Клик по цене в стакане**: Добавлена возможность кликнуть на любую цену в стакане, чтобы подставить её в поле ввода цены.
- [x] **Количество по умолчанию = 100000**: Изменено значение по умолчанию для более удобной работы.

### Документация:

- [x] **Полная переработка документации**: Обновлены все файлы документации для соответствия текущему состоянию проекта:
  - `CLAUDE.md` — расширен с полным описанием архитектуры и нюансов
  - `docs/ARCHITECTURE.md` — создан заново с диаграммами компонентов
  - `docs/API_SPEC.md` — дополнен описанием Trade модели и OrderSource
  - `docs/ROADMAP.md` — добавлена история изменений

---