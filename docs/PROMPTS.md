# 🚀 Gemini CLI Prompts

Промпты для запуска каждого этапа разработки.

---

## Общие инструкции

Перед запуском каждого этапа:

1. Перейди в директорию проекта:
   ```bash
   cd ~/projects/tinkoff-broker-emulator
   ```

2. Запусти Gemini CLI в YOLO mode для автоматического выполнения:
   ```bash
   gemini --yolo
   ```

3. Вставь соответствующий промпт

---

## Этап 0: Инициализация проекта

```
Инициализируй проект Spring Boot для эмулятора T-Invest API согласно GEMINI.md.

Задачи:
1. Создай структуру Gradle проекта (build.gradle.kts) с:
   - Spring Boot 4.0.0
   - Java 21
   - grpc-spring-boot-starter (net.devh:grpc-server-spring-boot-starter)
   - protobuf-gradle-plugin для генерации Java из proto
   - lombok
   - spring-boot-starter-web (для админки)
   - spring-boot-starter-websocket
   - junit 5, mockito

2. Скачай proto-контракты из https://github.com/RussianInvestments/investAPI/tree/main/src/docs/contracts
   Нам нужны: common.proto, marketdata.proto, orders.proto, operations.proto, instruments.proto

3. Настрой protobuf плагин для генерации Java классов в build/generated/source/proto

4. Создай application.yml с конфигурацией из GEMINI.md

5. Создай базовый docker-compose.yml

6. Создай .gitignore для Gradle/Java проекта

7. Создай README.md с описанием проекта и инструкцией запуска

8. Убедись, что проект компилируется: ./gradlew build

После завершения:
- Отметь выполненные задачи в docs/ROADMAP.md
- Оставь комментарий о проделанной работе
```

---

## Этап 1: Core Engine — OrderBook & Matching

```
Реализуй ядро биржевого движка согласно GEMINI.md и docs/MATCHING_ENGINE.md.

ВАЖНО: Перед началом работы выполни ./gradlew test и убедись, что все тесты проходят.

Задачи:

1. Создай пакет com.emulator.core.model с классами:
   - Order (id, instrumentId, direction, price, quantity, filledQuantity, status, createdAt, source)
   - OrderBook (instrumentId, bids, asks)
   - Trade (id, aggressorOrderId, passiveOrderId, price, quantity, timestamp)
   - PriceLevel (price, orders)
   - Enum OrderDirection, OrderStatus, OrderType, OrderSource

2. Создай пакет com.emulator.core.orderbook с:
   - OrderBookManager — thread-safe управление стаканом
     - addOrder(Order) — добавить заявку на уровень
     - removeOrder(orderId) — удалить заявку
     - getSnapshot(depth) — получить snapshot стакана
     - getBestBid() / getBestAsk()

3. Создай пакет com.emulator.core.matching с:
   - ProRataMatchingEngine — реализация Pro-Rata алгоритма
     - executeOrder(Order) — исполнение заявки
     - Алгоритм из docs/MATCHING_ENGINE.md

4. Напиши unit-тесты в src/test/java:
   - ProRataMatchingEngineTest с минимум 7 сценариями из MATCHING_ENGINE.md
   - OrderBookManagerTest

5. Все тесты должны проходить: ./gradlew test

После завершения:
- Отметь выполненные задачи в docs/ROADMAP.md
- Укажи покрытие тестами
```

---

## Этап 2: State Management — Account & Portfolio

```
Реализуй управление состоянием аккаунта согласно GEMINI.md.

ВАЖНО: Перед началом выполни ./gradlew test.

Задачи:

1. Создай пакет com.emulator.core.account с классами:
   - Account (id, balance, positions)
   - Position (instrumentId, quantity, averagePrice)
   - Portfolio (totalValue, positions, availableFunds)

2. Создай AccountManager:
   - Инициализация из конфигурации (application.yml)
   - updateBalance(amount) — изменение баланса
   - updatePosition(instrumentId, quantity, price) — обновление позиции
   - getPortfolioValue() — расчёт стоимости портфеля
   - calculateMaxLots(direction, price) — расчёт GetMaxLots с учётом маржи:
     - BUY: floor(portfolioValue × 7.0 / price)
     - SELL: floor(portfolioValue × 7.1 / price) + currentPosition

3. Создай слушатель событий для обновления аккаунта при сделках:
   - @EventListener для TradeExecutedEvent

4. Напиши unit-тесты:
   - AccountManagerTest
   - Тесты расчёта GetMaxLots

5. Все тесты должны проходить: ./gradlew test

После завершения обнови docs/ROADMAP.md.
```

---

## Этап 3.1: gRPC Services — Unary Methods

```
Реализуй gRPC сервисы (unary методы) согласно docs/API_SPEC.md.

ВАЖНО: Перед началом выполни ./gradlew test.

Задачи:

1. Создай gRPC Interceptor для авторизации:
   - Принимает любой Bearer token как валидный
   - Логирует входящие запросы

2. Реализуй MarketDataServiceImpl:
   - getOrderBook(request) — возврат стакана

3. Реализуй OrdersServiceImpl:
   - postOrder(request) — выставление заявки
   - cancelOrder(request) — отмена заявки
   - getOrders(request) — список активных заявок
   - getMaxLots(request) — расчёт максимума лотов

4. Реализуй OperationsServiceImpl:
   - getPortfolio(request) — информация о портфеле
   - getPositions(request) — позиции и средства

5. Реализуй InstrumentsServiceImpl:
   - findInstrument(request) — поиск TBRU

6. Создай утилиты для конвертации:
   - BigDecimal <-> Quotation
   - LocalDateTime <-> Timestamp

7. Напиши integration тесты с gRPC клиентом

8. Все тесты должны проходить: ./gradlew test

После завершения обнови docs/ROADMAP.md.
```

---

## Этап 3.2: gRPC Services — Streams

```
Реализуй gRPC стримы согласно docs/API_SPEC.md.

ВАЖНО: Перед началом выполни ./gradlew test.

Задачи:

1. Создай систему событий (Spring Events):
   - OrderBookChangedEvent
   - OrderStateChangedEvent
   - TradeExecutedEvent

2. Реализуй MarketDataStreamServiceImpl:
   - marketDataStream (bidirectional)
   - Подписка/отписка на стаканы
   - Публикация обновлений при изменении OrderBook

3. Реализуй OrdersStreamServiceImpl:
   - orderStateStream (server stream)
   - Публикация событий: NEW, PARTIALLY_FILLED, FILLED, CANCELLED

4. Создай StreamManager для управления подписками:
   - Хранение активных подписок
   - Рассылка событий подписчикам

5. Напиши integration тесты стримов

6. Все тесты должны проходить: ./gradlew test

После завершения обнови docs/ROADMAP.md.
```

---

## Этап 4: Web Admin UI

```
Реализуй веб-админку для ручного управления эмулятором.

ВАЖНО: Перед началом выполни ./gradlew test.

Задачи:

1. Создай REST контроллеры:
   - GET /api/orderbook — текущий стакан
   - GET /api/orders — список всех заявок
   - POST /api/orders — создание заявки (от участников рынка, не бота)
   - DELETE /api/orders/{id} — отмена заявки
   - GET /api/account — информация об аккаунте

2. Создай WebSocket endpoint:
   - /ws/orderbook — real-time обновления стакана

3. Создай SPA в src/main/resources/static:
   - index.html — главная страница
   - Визуализация стакана как в биржевом терминале
   - Форма создания заявки (тип, направление, цена, объём)
   - Формы НЕ очищаются после отправки
   - Real-time обновление через WebSocket
   - Список активных заявок с кнопкой отмены

4. Стилизация:
   - Цветовая индикация (зелёный bid, красный ask)
   - Стакан похож на терминал (см. скриншот в описании)
   - Responsive layout

5. Напиши тесты REST API

6. Все тесты должны проходить: ./gradlew test

После завершения обнови docs/ROADMAP.md.
```

---

## Этап 5: Docker & Integration

```
Финализируй проект: Docker, интеграционные тесты, документация.

ВАЖНО: Перед началом выполни ./gradlew test.

Задачи:

1. Создай Dockerfile:
   - Multi-stage build
   - Базовый образ: eclipse-temurin:21-jre-alpine
   - Оптимизация размера

2. Обнови docker-compose.yml:
   - Сервис эмулятора (порты 50051 для gRPC, 8080 для web)
   - Healthcheck
   - Volumes для application.yml

3. Создай E2E тесты:
   - Тест подключения gRPC клиента к контейнеру
   - Тест полного цикла: заявка → matching → стрим обновлений
   - Тест web-админки

4. Обнови README.md:
   - Полная инструкция запуска
   - Docker команды
   - Примеры конфигурации
   - Описание API

5. Финальная проверка:
   - docker-compose up --build
   - Все тесты: ./gradlew test
   - Интеграционные тесты с контейнером

После завершения:
- Отметь все задачи в docs/ROADMAP.md
- Напиши финальный комментарий о готовности проекта
```

---

## Быстрый старт всего проекта

Для запуска всех этапов последовательно (не рекомендуется, лучше по одному):

```bash
cd ~/projects/tinkoff-broker-emulator
gemini --yolo -p "Выполни все этапы из docs/ROADMAP.md последовательно. Начни с Этапа 0, после успешного завершения переходи к следующему. Перед каждым этапом запускай тесты. После каждого этапа обновляй ROADMAP.md."
```
